// Accent mark is now pure CSS via .bs-mark class (matching global site exactly)
// This component is kept for any JSX-based usage but the primary method is the CSS class
export default function Accent() { return null; }
