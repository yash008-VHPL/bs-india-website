import './Accent.css';

// Matches Berg+Schmidt global site: small red Γ-shaped bracket
// sits inline before heading text, NOT as a block above it
export default function Accent() {
  return <span className="bs-accent" aria-hidden="true" />;
}
